// todo: parse the markdown and place the topics in the vector

pub fn topics() -> Vec<String> {
    let topics = vec![
        "topic 1".to_string(),
        "topic 2".to_string(),
        "topic 3".to_string(),
        "topic 4".to_string(),
        "topic 5".to_string(),
    ];

    topics
}
